services:
  python3:
    restart: always
    build: .
    container_name: 'python3'
    # working_dir: /workspace/src
    tty: true
    environment:
      - PYTHONPATH=/workspace
    volumes:
      # - ./src:/workspace/src
      # - ./results:/workspace/results
      # - ./app:/workspace/app
      - ./:/workspace
